<?php

function unesco_frontpage_menu() {
  $items = array();

  $items['frontpage'] = array(
    'page callback' => 'unesco_frontpage_frontpage',
    'access callback' => TRUE,
    'type' => MENU_CALLBACK,
  );

  return $items;
}

function unesco_frontpage_frontpage () {
  return ' ';
}
